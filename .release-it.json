{
  "git": {
    "commitMessage": "chore(release): ${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true,
    "push": true
  },
  "github": {
    "release": true
  },
  "npm": {
    "publish": false
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "conventionalcommits",
      "infile": "CHANGELOG.md",
      "header": "# Changelog\n"
    }
  },
  "hooks": {
    "after:bump": "node scripts/sync-cargo-version.cjs ${version} && cargo metadata --format-version=1 >/dev/null && cargo metadata --manifest-path examples/serialport-test/src-tauri/Cargo.toml --format-version=1 >/dev/null && git add Cargo.toml Cargo.lock CHANGELOG.md package.json examples/serialport-test/src-tauri/Cargo.lock",
    "after:release": "echo \"Tagged v${version}. Next: pnpm whoami || pnpm login; then pnpm release:publish\""
  }
}
